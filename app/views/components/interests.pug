if resume.interests && resume.interests.length
  .container.interests-container
    +title("Interests", 'fa-light fa-heart')
    each interest in resume.interests
      section.item
        if interest.name
          h4.bold #{interest.name}
        if interest.keywords
          ul.minimal
            each keyword in interest.keywords
              li #{keyword}
